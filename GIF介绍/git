Git安装

##初始化Git仓库
-在项目目录右键打开git bash
-命令;git init

##自报家门
-就是在git中设置当前使用的用户是谁
-每一次备份都会把当前备份者的信息存储起来
-命令:
    配置用户用:git config --global user.name 'xiaoming'
    配置邮箱:git config --global user.name 'xiaoming@sina.com'

##查看状态
- git status

##查看日志
- git log --oneline

##退回到某一个时刻
- git reset --hard Head~0    0表示最新一次提交代码的时候
- git reset --hard 版权号    精确到某一个时刻